/*
 * @flow
 */
import * as React from 'react'
import Svg, {{getTagsString(children)}} from 'react-native-svg'

class {{name}} extends React.Component{
  render(){
    return (
<{{renderTag(tag)}}
{% if attr %}
{{renderProps(attr,2) | safe}}
{% endif %}
>
{{renderChildren(children) | safe}}
</{{renderTag(tag)}}>
    )
  }
}

export default {{name}}
